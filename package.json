{
  "name": "claudeclaw",
  "version": "0.1.0",
  "description": "Telegram chatbot with per-chat Claude Agent SDK sessions",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "NODE_OPTIONS=--dns-result-order=ipv4first tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "NODE_OPTIONS=--dns-result-order=ipv4first node dist/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.81",
    "dotenv": "^17.3.1",
    "telegraf": "^4.16.3",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.20.6",
    "typescript": "^6.0.2"
  }
}
